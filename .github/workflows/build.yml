name: Build
on:
  push:
    branches: [main]
    paths: ['src/**', 'Cargo.*']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release

      - name: Run tests
        run: cargo test

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          files: target/release/cbnu-notice-bot
          prerelease: true
          body: |
            자동 빌드 바이너리 (crawl.yml에서 사용)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
